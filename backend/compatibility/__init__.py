"""Compatibility boundary.

Bridges the legacy ``app.*`` prototype into the target architecture without
redirecting live runtime traffic at Gate 02B. Adapter contracts are documented
in ``legacy_adapters.py``; implementations arrive in a later gate.
"""